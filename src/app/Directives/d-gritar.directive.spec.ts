import { DGritarDirective } from './d-gritar.directive';

describe('DGritarDirective', () => {
  it('should create an instance', () => {
    const directive = new DGritarDirective();
    expect(directive).toBeTruthy();
  });
});
